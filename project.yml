name: LynQ
options:
  bundleIdPrefix: com.lynq
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16"

settings:
  base:
    SWIFT_VERSION: "5.9"
    ALWAYS_SEARCH_USER_PATHS: NO
    CLANG_ENABLE_MODULES: YES
    ENABLE_TESTABILITY: NO

targets:
  LynQ:
    type: application
    platform: iOS
    sources:
      - path: ios_app
        type: group
    settings:
      base:
        PRODUCT_NAME: LynQ
        PRODUCT_BUNDLE_IDENTIFIER: com.lynq.app
        INFOPLIST_FILE: ios_app/Info.plist
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: $(DEVELOPMENT_TEAM)
        ANTHROPIC_API_KEY: $(ANTHROPIC_API_KEY)
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: NO
    entitlements:
      path: ios_app/LynQ.entitlements
      properties:
        com.apple.developer.in-app-payments:
          - com.lynq.app.pro.monthly
          - com.lynq.app.pro.annual
          - com.lynq.app.lifetime
          - com.lynq.app.business.monthly
          - com.lynq.app.business.annual
    capabilities:
      - camera
      - in-app-purchase
